import React from "react";
import LoginScreen from '../Login/Loginscreen';

const divStyle ={
  textAlign: "center"
}

const Login = () => (
  <div style={divStyle}>
  	<LoginScreen />
  </div>
);

export default Login;
